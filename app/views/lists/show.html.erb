<h2><%= @list.name %></h2>

<%= link_to "New Bookmark", new_list_bookmark_path(@list) , class: "btn btn-success" %> |

<table>
<% @list.bookmarks.each do |bookmark| %>
  <div> <%= bookmark.movie.title %> | <%= bookmark.movie.overview %> | <%= link_to "Remove", bookmark_path(bookmark.id), method: :delete, data: { confirm: "Are you sure?" } %>
  </div>
  <% end %>
  </table>
<%= link_to "Back", lists_path, class: "btn btn-primary btn-sm"%>
